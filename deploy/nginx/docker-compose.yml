services:
  discocs-frontend:
    build:
      context: ../..
      dockerfile: deploy/nginx/Dockerfile
    restart: unless-stopped
    ports:
      - "${DISCOCS_HTTP_PORT:-80}:8080"
    environment:
      BACKEND_HOST: ${BACKEND_HOST:-127.0.0.1}
      BACKEND_PORT: ${BACKEND_PORT:-7752}
