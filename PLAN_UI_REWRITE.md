# UI Rewrite — Phased Plan

See `PLAN_UI_ARCHITECTURE.md` for architecture decisions, component map, and stack rationale.

---

## Phase 0 — Project setup

**Goal:** running dev server, blank page, proxy working.

- [ ] Init `ui/` with `npm create vite@latest -- --template react-ts`
- [ ] Install Tailwind CSS v4 + shadcn/ui: `npx shadcn@latest init` with answers:
  - Style → **New York**
  - Base color → **Zinc**
  - CSS variables for theming → **Yes**
  - All other prompts (paths, aliases) → accept defaults
- [ ] Install deps: `react-router-dom`, `@tanstack/react-query`, `zustand`
- [ ] Install openapi-typescript: `npm install -D openapi-typescript` — generates TypeScript types from FastAPI's `/openapi.json`; run with `npx openapi-typescript http://localhost:7752/openapi.json -o src/api/schema.d.ts` and re-run whenever the API changes
- [ ] Add shadcn components needed from the start: `npx shadcn@latest add button dialog slider dropdown-menu popover tabs tooltip scroll-area skeleton badge`
- [ ] `vite.config.ts`: proxy the following paths to `http://localhost:7752`:
  `/api`, `/tracks`, `/artists`, `/releases`, `/mixes`, `/settings`, `/stats`, `/jobs`, `/feedback`, `/navidrome`, `/dashboard`
- [ ] `index.css`: configure shadcn theme variables to match discocs color palette (see `PLAN_UI_ARCHITECTURE.md` § Styles)
- [ ] `main.tsx`: mount React, wrap with `QueryClientProvider` + `RouterProvider`
- [ ] Backend: add `/app` catch-all route in `main.py` serving `ui/dist/index.html` (new UI, no impact on existing routes)
- [ ] Backend: rename old UI routes from `/` → `/admin`, add warning banner HTML to `ui.html`

**Done when:** `vite dev` runs, visiting `/app` shows a blank dark page; shadcn `<Button>` renders with correct accent color.

---

## Phase 1 — API client + Query hooks

**Goal:** typed API layer, no UI yet.

- [ ] `api/client.ts` — `apiFetch<T>(url, options?)`: typed wrapper, throws on non-2xx, JSON parse. Types come from `src/api/schema.d.ts` (generated by openapi-typescript)
- [ ] `api/dashboard.ts` — `fetchDashboard(limit: number)`
- [ ] `api/search.ts` — `fetchSearch(query: string)`
- [ ] `api/artists.ts` — `fetchArtist(id)`, `fetchArtistDiscography(id)`, `fetchArtistTopTracks(id)`
- [ ] `api/releases.ts` — `fetchRelease(id)`, `fetchReleaseTracks(id)`, `fetchReleaseRelated(id)`, `fetchReleaseRecommendations(id)`
- [ ] `api/mixes.ts` — `fetchMix(id)`, `fetchMixTracks(id)`, `saveMix(id)`
- [ ] `api/playback.ts` — `createSession(type, id, label, preferredTrackId?)`, `fetchQueue(sessionId)`, `postEvent(payload)`, `refillAutoplay(payload)`, `patchSession(id, patch)`
- [ ] `api/settings.ts` — `fetchNavidromeSettings()`, `saveNavidromeSettings(data)`, `pingNavidrome()`
- [ ] `api/hooks/` — one `useQuery` hook per fetch function above
- [ ] `api/hooks/useNavidromeStatus.ts` — checks settings on mount, returns `connected | disconnected | unknown`

**Done when:** hooks can be imported and used; tested manually via console or a throwaway component.

---

## Phase 2 — AudioEngine + Player store

**Goal:** music plays from the browser console, store is wired up.

- [ ] `engine/AudioEngine.ts`
  - Creates `HTMLAudioElement`, `preload="none"`
  - `load(url)`, `play()`, `pause()`, `seek(fraction)`, `setVolume(v)`, `setMuted(b)`
  - On `timeupdate` → `playerStore.setTime(currentTime, duration)`
  - On `ended` → `playerStore.handleTrackEnded()`
  - On `error` → `playerStore.setError(message)`
  - On `play` / `pause` → `playerStore.setPlaybackState(state)`
  - Media Session API: set metadata on play, register action handlers
- [ ] `store/playerStore.ts` (Zustand)
  - State: `session`, `queue`, `currentTrackId`, `currentQueueItemId`, `playbackState`, `currentTime`, `duration`, `volume`, `muted`, `error`, `expanded`
  - Actions: `playSource`, `playTrack`, `togglePlay`, `seek`, `skipNext`, `skipPrevious`, `toggleShuffle`, `toggleRepeatOne`, `toggleAutoplay`, `setAutoplayChip`, `setVolume`, `toggleMute`, `refreshQueue`, `recordEvent`, `handleTrackEnded`, `toggleExpanded`
  - Volume + mute persisted to localStorage on change
- [ ] `store/navidromeStore.ts` — liked track IDs set, `toggleLike(trackId)`, `fetchLikedIds()`

**Done when:** calling `playerStore.getState().playSource('artist', 1, 'Test')` in console plays music.

---

## Phase 3 — Layout shell

**Goal:** sidebar + topbar + content area + player bar placeholder visible.

- [ ] `components/layout/AppShell.tsx` — grid: sidebar | content-area, padding-bottom for player
- [ ] `components/layout/Sidebar.tsx` — nav links to `/`, `/search`; active state via `useLocation`
- [ ] `components/layout/TopBar.tsx` — right slot for profile button
- [ ] `pages/` stub components for each route (just `<h1>` placeholders)
- [ ] Router setup: `App.tsx` with `<Routes>` for `/`, `/search`, `/artists/:id`, `/releases/:id`, `/mixes/:id`
- [ ] `components/player/PlayerBar.tsx` stub — fixed bottom bar, correct height, background color
- [ ] `components/player/ExpandedPlayer.tsx` stub — overlay hidden by default

**Done when:** navigating between routes changes the page title; sidebar highlights the active link; player bar occupies the bottom.

---

## Phase 4 — Shared media components

**Goal:** MediaCard, Shelf, TrackRow, TrackTable built and tested in isolation.

- [ ] `components/media/ArtworkImage.tsx` — img with `onError` fallback to placeholder letter
- [ ] `components/media/MediaCard.tsx` — artwork, title, subtitle, optional reason text, play button, click → navigate
- [ ] `components/media/Shelf.tsx` — heading + subtitle + total count + horizontal scroll row of MediaCards
- [ ] `components/media/TrackRow.tsx` — #, play button, cover, title+artists, release link, duration, menu trigger
- [ ] `components/media/TrackTable.tsx` — `<table>` wrapper around TrackRows
- [ ] `components/media/TrackMenu.tsx` — Radix DropdownMenu: "Play", "Play next", "Go to artist", "Go to release"

**Done when:** can render `<Shelf items={mockData} />` and `<TrackTable tracks={mockTracks} />` and they look correct.

---

## Phase 5 — Pages

**Goal:** all five pages render real data.

### Dashboard
- [ ] `pages/DashboardPage.tsx`
  - `useDashboard()` hook
  - Search bar (controlled input, Enter → navigate to `/search?q=...`)
  - "Flow" placeholder card (disabled, same as current)
  - Grid of `<Shelf>` components from API response

### Search
- [ ] `pages/SearchPage.tsx`
  - Read `q` from URL params
  - `useSearch(q)` hook
  - Tabs: All / Artists / Releases / Tracks (Radix Tabs or manual)
  - Results: MediaCards for artists/releases, TrackTable for tracks

### Artist
- [ ] `pages/ArtistPage.tsx`
  - Parallel fetch: `useArtist(id)` + `useArtistDiscography(id)` + `useArtistTopTracks(id)`
  - Surface header: avatar, name, stats, Play button, Search button
  - Top Tracks shelf (TrackTable)
  - Discography groups as Shelves (albums, singles, etc.)
  - Tabs stub: Discography (active) | Similar Artists (disabled)

### Release
- [ ] `pages/ReleasePage.tsx`
  - Parallel fetch: release + tracks + related + recommendations
  - Surface header: artwork, title, artists, year, duration, Play button
  - Track list (TrackTable with releaseContextId)
  - Related discography shelf
  - Recommended releases shelf

### Mix
- [ ] `pages/MixPage.tsx`
  - Fetch mix + tracks
  - Header: title, status, Play button, Save button
  - Track list

**Done when:** all five pages render real library data end to end.

---

## Phase 6 — Player bar (full)

**Goal:** fully working player, all controls wired to store.

- [ ] `PlayerBar.tsx` full implementation
  - Seek slider (Radix Slider) — driven by `currentTime / duration`, `onValueChange` → `store.seek()`
  - Time bubble on hover (port from current CSS)
  - Controls: Prev, Play/Pause, Next — wired to store actions
  - Now-playing section: cover art (`ArtworkImage`), track title, artist/release links
  - Like button (Radix Tooltip) — `navidromeStore.toggleLike(currentTrackId)`
  - Dislike button → `store.recordEvent('disliked')`
  - Three-dot menu → `TrackMenu`
  - Volume: mute toggle + Radix Slider
  - Repeat One toggle
  - Shuffle toggle
  - Expand button → `store.toggleExpanded()`
  - Error message area
- [ ] `ExpandedPlayer.tsx` full implementation
  - Full-width artwork
  - Track title + subtitle large
  - Queue panel: source label, autoplay toggle, autoplay status, chip row, queue list
  - Queue items: `QueueItem.tsx` — active state, click → `store.playTrack()`
  - Autoplay pool section (prepared items, dimmed)
  - Close button
- [ ] `QueueItem.tsx`
- [ ] `AutoplayChips.tsx` — "All", artist-based, release-based chips

**Done when:** full playback cycle works — play → queue shows → autoplay refills → skip → like → queue jumps.

---

## Phase 7 — Profile & Navidrome settings

**Goal:** profile button in TopBar, settings reachable, connection status visible.

- [ ] `components/profile/ProfileButton.tsx` — avatar/icon button in TopBar, Radix Popover trigger
- [ ] `components/profile/ProfilePanel.tsx`
  - Navidrome connection status dot + label
  - "Open settings" link → opens SettingsModal
- [ ] `components/profile/SettingsModal.tsx` — Radix Dialog
  - Navidrome URL field
  - Username field
  - Password field (write-only, placeholder "leave blank to keep saved")
  - Auth mode select
  - Test connection button → `pingNavidrome()` → success/error feedback
  - Save button
- [ ] `useNavidromeStatus` — poll on mount, re-check after save

**Done when:** can enter Navidrome credentials, test, save, and see connected status dot turn green.

---

## Phase 8 — Polish & responsive

**Goal:** looks good at all sizes, keyboard works, feels fast.

- [ ] Responsive sidebar: collapses to bottom tab bar on narrow viewports (mobile)
- [ ] TopBar hidden on mobile (nav in tab bar)
- [ ] Skeleton screens via shadcn `<Skeleton>` for page loading states
- [ ] Error boundary per page with retry button
- [ ] Keyboard shortcuts: Space (play/pause), Left/Right arrows (seek ±10s), M (mute)
- [ ] Page transitions (fade on route change)
- [ ] Expanded player slide-up animation
- [ ] `<title>` updates to current track name
- [ ] Favicon
- [ ] Scrollbar styling in `index.css`

---

## Phase 9 — Backend cutover

**Goal:** new UI is the default; old UI accessible at `/legacy`.

- [ ] `main.py`: move old HTML response routes to `/legacy`, `/legacy/search`, etc.
- [ ] `main.py`: new catch-all at `/` and `/{path:path}` serves `ui/dist/index.html`
- [ ] `main.py`: mount `ui/dist/assets` as StaticFiles at `/assets`
- [ ] `ui/vite.config.ts`: set `base: '/'`
- [ ] CI/CD: `npm run build` in `ui/` as part of deployment
- [ ] Remove `UI_HTML = ...` from `main.py` once old routes are retired

**Done when:** visiting `/` shows the new React UI in production.

---

## Phase 10 — Capacitor (iOS/Android)

- [ ] `npm install @capacitor/core @capacitor/cli @capacitor/ios @capacitor/android`
- [ ] `npx cap init discocs com.discocs.app --web-dir ui/dist`
- [ ] `capacitor.config.ts`: backend URL configurable via profile settings (stored in Capacitor preferences)
- [ ] Handle safe area insets: CSS `env(safe-area-inset-*)` on PlayerBar and Sidebar
- [ ] `@capacitor/status-bar`: match status bar color to `--surface-0`
- [ ] `npx cap add ios && npx cap add android`
- [ ] Test on simulator

---

## Phase 11 — Electron

- [ ] Init with `electron-vite` (or add Electron to existing `ui/` package)
- [ ] Main process: load `http://localhost:<port>` if backend is already running, or launch FastAPI as child process
- [ ] Window: frameless optional, custom title bar
- [ ] IPC: expose backend port to renderer if needed
- [ ] `electron-updater` for auto-update

---

## Settled decisions

- **Routing:** React Router v7 loaders — data loads before render, no loading flash on navigation
- **Navidrome likes:** fetch on track change, cache in `navidromeStore` — no polling
- **Search:** 300ms debounce on input + Enter also triggers immediately
- **Styles:** Tailwind CSS v4 + shadcn/ui, theme variables mapped to discocs palette
- **Admin panel:** old UI moves to `/admin` with visible warning banner, no login required
